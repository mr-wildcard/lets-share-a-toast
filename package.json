{
  "private": true,
  "scripts": {
    "dev:web": "cd web && pnpm dev",
    "dev:firebase": "cd firebase && concurrently pnpm:dev:*",
    "dev": "concurrently pnpm:dev:*"
  },
  "devDependencies": {
    "concurrently": "^7.5.0"
  }
}
