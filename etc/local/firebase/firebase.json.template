{
  "firestore": {
    "rules": "firestore.rules"
  },
  "emulators": {
    "auth": {
      "host": "0.0.0.0",
      "port": "$AUTH_PORT"
    },
    "firestore": {
      "host": "0.0.0.0",
      "port": "$FIRESTORE_PORT"
    },
    "ui": {
      "enabled": true,
      "host": "0.0.0.0",
      "port": "$UI_PORT"
    }
  }
}
